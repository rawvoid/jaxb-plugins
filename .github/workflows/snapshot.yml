name: 'CI: Deployment to Maven Central (Snapshot)'

on:
  push:
    branches: [ main, master ]

jobs:
  publish:
    name: Build and Deploy Snapshot
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
          server-id: central
          server-username: MAVEN_CENTRAL_USERNAME
          server-password: MAVEN_CENTRAL_PASSWORD

      - name: Extract project version
        id: version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Current POM version: $VERSION"

      - name: Validate version type
        id: check-snapshot
        run: |
          VERSION=${{ steps.version.outputs.version }}
          if [[ "$VERSION" == *"-SNAPSHOT" ]]; then
            echo "✅ Valid snapshot version detected."
            echo "is_snapshot=true" >> $GITHUB_OUTPUT
          else
            echo "::warning::Non-SNAPSHOT version detected ($VERSION) on main branch. Skipping snapshot deployment."
            echo "is_snapshot=false" >> $GITHUB_OUTPUT
          fi

      - name: Deploy to Sonatype Snapshots
        if: steps.check-snapshot.outputs.is_snapshot == 'true'
        run: |
          echo "🚀 Deploying artifact to Maven Central Snapshot Repository..."
          mvn clean deploy -Dgpg.skip
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}

      - name: Deployment Info
        if: steps.check-snapshot.outputs.is_snapshot == 'false'
        run: |
          echo "### ⏭️ Snapshot Deployment Skipped" >> $GITHUB_STEP_SUMMARY
          echo "The current version **${{ steps.version.outputs.version }}** is a Release version." >> $GITHUB_STEP_SUMMARY
          echo "To publish a release, please create a git tag instead." >> $GITHUB_STEP_SUMMARY
